// import {api} from '..';

// import {ReqDTO, ResDTO} from './types';

// export const request = async (body: ReqDTO) => {
//   const {data} = await api.post<ResDTO>('', body);

//   return data;
// };

// export const request = async (body: ReqDTO) => {
//   const {data} = await api.get<ResDTO>(`?${String(new URLSearchParams(body))}`);

//   return data;
// };
